using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneTracker : MonoBehaviour
{
    // Start is called before the first frame update
    public static bool returnTitleScene;
    public static bool ssToIG;
    public static bool cleaToIG;
    void Start()
    {
        returnTitleScene = false;
        ssToIG = false;
        cleaToIG = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
